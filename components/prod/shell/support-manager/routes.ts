const supportManagerApiRoutes = {
  accountLookup: "/dashboard/accounts/lookup",
} as const;

const supportManagerWebRoutes = {
  view: "/support-manager",
} as const;

export { supportManagerApiRoutes, supportManagerWebRoutes };
